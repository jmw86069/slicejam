# TODO items for Slicejam

## Enhancements

1. Export peak audit trail of some kind, that describes what happens
to each peak. For example, filtered out by MGM, differential/unchanged, etc.

2. Export per-peak group mean values

   * raw, normalized, normalized-batch-adjusted
   * could be convenient place to add flags like "MGM peak", or "active promoter peak"
