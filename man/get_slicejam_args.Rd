% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slicejam_util.R
\name{get_slicejam_args}
\alias{get_slicejam_args}
\title{Get slicejam argument values}
\usage{
get_slicejam_args(
  use_first_fc = TRUE,
  verbose = FALSE,
  type = c("printDebug", "data.frame"),
  name = c("analysis", "eval"),
  ...
)
}
\description{
Get slicejam argument values
}
\details{
This function is a convenient wrapper to handle conversion
of argument values sent via environmental variables, into
values to be used during slicejam analysis.

Input can be via environmental variables, or through direct
definition using \code{...} function arguments. In this case,
the direct function argument is used when available, and
environmental variable is used otherwise.
For example:
\code{get_slicejam_args(FC_FILE="test.fc")} will define \code{FC_FILE="test.fc"}
and will not use \code{Sys.getenv("FC_FILE")}.

Recognized arguments:
\itemize{
\item FC_FILE - path to featureCounts .fc file
\item CURATION_TXT - path to tab-delimited curation.txt file
\item GTF - path to GTF file
\item GTFNAME - optional short GTF name
\item MASK - path to optional BED file with MASK regions
\item DRYRUN - 1/0 whether to enable dry-run mode
\item OUTDIR - optional output directory
\item ATAC - 1/0 whether to enable ATAC mode
\item MGM - numeric max group mean threshold
\item NORM - normalization method: quantile, median, mediangroup, none
\item NORMMIN - normalization minimum_mean used for median, mediangroup
\item GROUPCHECK - 1/0 whether to stop analysis after groups and contrasts
\item SAVE_RDATA - 1/0 whether to save RData during processing
\item UPSTREAM_PROMOTER - default 2000
\item DOWNSTREAM_PROMOTER - default 200
\item UPSTREAM_TTS - default 1000
\item DOWNSTREAM_TTS - default 1000
\item DETECTED_TX - optional path to detected transcripts, one per line
\item DETECTED_GENES - optional path to detected genes, one per line
}

Returned arguments derived from input:
\itemize{
\item fc_file - valid fc_file
\item fc_filepath - valid full path to featureCounts file
\item NORMSHORT - short normalization name
\item fc_base - base filename derived from featureCounts filename
\item fc_basedir - base directory derived from featureCounts filename
\item fc_html - output HTML filename
\item knit_root_dir - full path to directory used for knitr output
\item cache_dir - directory used for knitr file cache
}
}
