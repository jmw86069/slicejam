% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slicejam-piecharts.R
\name{peak_pie_by_region}
\alias{peak_pie_by_region}
\title{Pie chart of feature_type values, wrapper to feature_to_pie()}
\usage{
peak_pie_by_region(
  hit_array,
  feature_type_winner = NULL,
  PeakSet = "",
  iHit = NULL,
  iSig = NULL,
  iCon = NULL,
  iPeaks = NULL,
  do_pie = TRUE,
  main = NULL,
  colorSub = NULL,
  ...
)
}
\arguments{
\item{hit_array}{\code{array} output from \code{se_contrast_stats()} with
three dimensions: hit, contrasts, signal. The arguments
\code{iHit}, \code{iCon}, and \code{iSig} are used to subset the array for
a specific result to use in each pie chart. Each cell of the
array is expected to contain a numeric list with values \code{c(-1, 1)}
with names that are peak names.}

\item{feature_type_winner}{\code{character} or \code{factor} vector whose names
are expected to be peak names, which are also names of entries
in \code{hit_array}.}

\item{iHit, iCon, iSig}{\code{character} value indicating a specific hit,
contrast, signal, respectively.}

\item{iPeaks}{\code{character} vector with specific peaks to use, instead of
using values in \code{hit_array}.}

\item{main}{\code{character} string used as the plot title.}

\item{colorSub}{\code{character} vector of R colors, whose names are intended
to match values in \code{x}.}

\item{...}{additional arguments are passed to \code{feature_to_pie()}.}
}
\description{
Pie chart of feature_type values, wrapper to feature_to_pie()
}
\details{
This function is called by \code{slicejam_analysis.Rmd} and is intended
mainly as a wrapper to \code{feature_to_pie()} in order to prepare
multiple pie charts in one multi-panel figure.

Note this function may be replaced with a ggplot2 equivalent, in
order to use ggplot2 common color key to help provide more
legible labels.
}
\seealso{
Other slicejam plot functions: 
\code{\link{feature_to_pie}()},
\code{\link{gg_pie_by_feature_type}()}
}
\concept{slicejam plot functions}
