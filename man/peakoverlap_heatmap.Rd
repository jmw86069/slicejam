% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slicejam_overlap.R
\name{peakoverlap_heatmap}
\alias{peakoverlap_heatmap}
\title{Heatmap of peak overlap statistics}
\usage{
peakoverlap_heatmap(
  m,
  col = "Reds",
  row_split = NULL,
  breaks = NULL,
  show_breaks = NULL,
  col_hm = NULL,
  lens = 2,
  cluster_rows = FALSE,
  cluster_columns = FALSE,
  m2 = NULL,
  prefix = "",
  suffix = "",
  show_label = TRUE,
  label_cex = 1,
  abbrev = TRUE,
  cell_fun = NULL,
  cell_outline = FALSE,
  ...
)
}
\arguments{
\item{m}{\code{numeric} matrix}

\item{col}{any input compatible with \code{colorjam::getColorRamp()} which
includes: \code{character} string of a specific color gradient
from \code{RColorBrewer} or \code{viridis}; \code{character} vector of
R colors.}

\item{row_split}{\code{NULL}, or vector of \code{character} or \code{factor} values
passed to \code{ComplexHeatmap::Heatmap()} argument \code{row_split} and
\code{column_split} to split rows and columns into groups.}

\item{breaks}{\code{numeric} vector that defines color breaks used in
the color mapping function \code{circlize::colorRamp2()}.}

\item{show_breaks}{\code{numeric} vector the defines color break positions
to display in the color legend. When \code{show_breaks=NULL} it uses
\code{breaks} by default.}

\item{col_hm}{\code{NULL} or \code{function} generated by \code{circlize::colorRamp2()}
that maps specific numeric ranges to specific colors. When \code{col_hm}
is supplied, \code{col} is ignored.}

\item{lens}{\code{numeric} value passed to \code{colorjam::getColorRamp()} which
defines any compression or expansion of the color gradient, where
values higher than 1 make colors more intense, and values less than
-1 make colors less intense across the color range.}

\item{cluster_rows, cluster_columns}{\code{logical} indicating whether
to cluster and thus re-order rows and columns.}

\item{m2}{\code{matrix} or \code{list} of \code{matrix} objects. When supplied,
each \code{matrix} is used to add numeric labels to each cell,
relevant only when \code{show_label=TRUE}.}

\item{prefix, suffix}{\code{character} vector of prefix and suffix
values to add to each matrix value displayed when
\code{show_label=TRUE}. The purpose is to add a label describing the
type of value, for example \code{suffix="j"} may help reinforce that
a numeric value is a Jaccard overlap coefficient; \code{suffix="\%"}
may indicate a percentage. Note that these labels are applied
to \code{m} and each matrix in \code{m2} in order.}

\item{show_label}{\code{logical} indicating whether to define
\code{cell_fun=cell_fun_label()} to display cell labels in the heatmap.}

\item{label_cex}{\code{numeric} value passed to \code{cell_fun_label()} to
adjust the fontsize in for cell labels, as relevant.}

\item{abbrev}{\code{logical} passed to \code{cell_fun_label()} as needed,
which determines whether the numeric value should be abbreviated
for every \verb{1,000} unit of measure. For example 1,500 will be
labeled \verb{1.5k}, and 21,300,000 will be labeled \verb{21.3M}.}

\item{cell_fun}{\code{function} or when \code{cell_fun=NULL} and \code{show_label=TRUE}
a custom function \code{cell_fun_label()} will be used that displays the
numeric value in \code{m} for each cell. If \code{m2} is also supplied then the
value from each \code{matrix} in \code{m2} will also be displayed in each
cell. To force no \code{cell_fun} to be used, set \code{cell_fun=FALSE}, or
set \code{show_label=FALSE} and \code{cell_fun=NULL}.}

\item{cell_outline}{\code{logical} passed to \code{cell_fun_label()} which is
used only when \code{show_label=TRUE}. This argument defines whether an
outline is drawn around each heatmap cell.}
}
\value{
\code{ComplexHeatmap::Heatmap} object. Use \code{ComplexHeatmap::draw()}
or simply \code{print()} to display the heatmap.
}
\description{
Heatmap of peak overlap statistics
}
\details{
This function is a method to create a heatmap for the output
of \code{peakoverlap_calcs()}.

See \code{peakoverlap_calcs()} for the full example workflow.
}
\seealso{
Other slicejam overlap:
\code{\link[=peakoverlap_calcs]{peakoverlap_calcs()}}
}
\concept{slicejam overlap}
