% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slicejam_venn.R
\name{match.im}
\alias{match.im}
\title{match incidence matrix to incidence matrix}
\usage{
match.im(x, table, MARGIN = 1L, ...)
}
\description{
match incidence matrix to incidence matrix
}
\details{
This function is an optimized case of \code{match.matrix()}
that requires the matrix values to be \code{c(-1, 0, 1)}
such that each row can be condensed to a single integer
value, and a signed variant of that integer value.

Once converted, the \code{match()} operation is quite fast.

There are alternatives to \code{match.matrix()}, first is
to convert the \code{matrix} to \code{data.frame}, which itself is
somewhat slow. Next, run \code{match.data.frame()} which is
also slow, because it ultimately runs \code{match()} on two
\code{list} objects.
}
\examples{
ny <- 1000000;
nx <- 4;
x <- matrix(ncol=nx, nrow=ny, data=sample(c(-1,0,1), size=nx*ny, replace=TRUE));
x_u <- unique(x);
system.time(u_match <- match.im(x, x_u));
## 1 second
system.time(u_match1 <- match.matrix(x, x_u));
## 7 seconds
table(u_match1 == u_match);

x_df <- data.frame(x);
x_u_df <- data.frame(x_u);
system.time(u_match2 <- match(asplit(x_df, 1), asplit(x_u_df, 1)));
## 10 seconds
table(u_match2 == u_match);

}
