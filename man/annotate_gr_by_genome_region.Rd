% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slicejam_genomic.R
\name{annotate_gr_by_genome_region}
\alias{annotate_gr_by_genome_region}
\title{Annotate GRanges by genome_regions}
\usage{
annotate_gr_by_genome_region(
  gr,
  genome_regions,
  name_colname = "name",
  gene_name_colname = "gene_name",
  feature_type_colname = "feature_type",
  gene_id_colname = "gene_id",
  mask_regions = NULL,
  feature_grep_order = c("promoter", "tts", "exon", "intron", "extragenic", "intergenic",
    "."),
  include_type = c("overlap", "winner", "nearest_gene"),
  sort_optimization = c("fast", "global"),
  verbose = FALSE,
  ...
)
}
\arguments{
\item{gr}{\code{GRanges} object to be annotated. The \code{values(gr)}
should contain a colname that matches \code{name_colname},
otherwise names will be created for each entry in
colname \code{"name"}, see \code{name_colname} below.}

\item{genome_regions}{\code{GRanges} object as produced by
\code{genomic_regions_from_gtf()}. The \code{values(genome_regions)}
should contain a colname that matches \code{gene_name_colname},
and the feature type should be stored in a colname
\code{feature_type_colname}.}

\item{name_colname}{\code{character} value that matches one colname
in \code{values(gr)}. When no colname is supplied, a colname \code{"name"}
is created with dummy values with the format \code{"gr00001"}.}

\item{gene_name_colname}{\code{character} value that matches one colname
in \code{values(genome_regions)}.}

\item{feature_type_colname}{\code{character} value that matches one colname
in \code{values(genome_regions)}, and which contains feature types.}

\item{gene_id_colname}{\code{character} optional value of one colname in
\code{values(genome_regions)} to be included alongside gene annotations.}

\item{mask_regions}{one of the following input formats:
\itemize{
\item \code{character} vector with file or files that contain mask regions
in BED format, only the regions are retained without further annotation
\item \code{GRanges} object containing mask regions. Currently the name is ignored.
\item \code{GRangesList} object, which is used as \code{mask_regions@unlistData} to
convert that format to \code{GRanges} for internal use. No other annotations
are used, all regions are considered \code{"mask"}.
}}

\item{feature_grep_order}{\code{character} vector of grep patterns used
to match values in \code{feature_type_colname}, to define the priority
of feature types to use for the "winner".}

\item{include_type}{\code{character} vector indicating which of the three
annotation phases to include:
\enumerate{
\item \code{"overlap"} annotates each region by direct overlap with \code{genome_regions}
\item \code{"winner"} annotates each region by direct overlap, using only
the feature_type_winner for each entry in \code{gr}.
\item \code{"nearest_gene"} annotates each entry in \code{gr} by the nearest gene
in \code{genome_regions} alongside the distance to nearest gene.
}}

\item{verbose}{\code{logical} indicating whether to print verbose output.
More detailed output is printed when \code{verbose=2}.}

\item{...}{additional arguments are ignored.}
}
\description{
Annotate GRanges by genome_regions, then define 'winner' using
ordered feature type values, with corresponding gene data.
Also define nearest gene for each feature.
}
\details{
This function uses the 'genome_regions' \code{GRanges} data from
\code{genomic_regions_from_gtf()} to annotate 'gr' \code{GRanges}.

It applies basic overlap, then heuristic rules to
assign best feature type per region. For example, a
transcript factor binding region may overlap a gene
promoter, and perhaps part of the first exon of that gene.
The output will include \code{feature_type='promoters,exon'},
and \code{feature_type_winner='promoters'}.
Similarly, a region that overlaps 'exon' and 'intron'
will have \code{feature_type='exon,intron'} and
\code{feature_type_winner='exon'}.

It performs three levels of annotation:
\enumerate{
\item Direct overlap. Any overlapping region in \code{genome_regions}
is added as an annotation column, where multiple regions
are concatenated by commas.
\item Define 'winner'. When there are multiple overlapping
\code{feature_type}, use the best observed type.
It applies this filter to the gene associations,
and assigns new columns with suffix \code{'_winner'}.
\item Nearest gene. The nearest gene body, prioritizing
direct overlap, then lowest distance to gene body.
It uses column prefix \code{'nearest_'}, with distance
stored in \code{'nearest_gene_distance'}.
}

When \code{mask_regions} is supplied, one additional column is
added \code{"mask_region"} with either \code{TRUE} or \code{FALSE}.
The identity of the region is not stored, only the
\code{logical} value of whether there is any overlap.
}
\seealso{
Other slicejam genome regions:
\code{\link[=flatten_genome_regions]{flatten_genome_regions()}},
\code{\link[=genomic_regions_from_gtf]{genomic_regions_from_gtf()}},
\code{\link[=genomic_regions_from_gtf_bed]{genomic_regions_from_gtf_bed()}},
\code{\link[=promoter_to_tss]{promoter_to_tss()}},
\code{\link[=salmon_tss_per_gene]{salmon_tss_per_gene()}}
}
\concept{slicejam genome regions}
