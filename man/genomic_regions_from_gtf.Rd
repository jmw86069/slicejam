% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slicejam_genomic.R
\name{genomic_regions_from_gtf}
\alias{genomic_regions_from_gtf}
\title{Prepare genomic regions from GTF}
\usage{
genomic_regions_from_gtf(
  gtf = NULL,
  upstream_promoter = 2000,
  downstream_promoter = 200,
  upstream_tts = 1000,
  downstream_tts = 1000,
  detectedTx = NULL,
  detectedGenes = NULL,
  geneAttrNames = c("gene_id", "gene_name"),
  txAttrNames = c("transcript_id"),
  geneFeatureType = "exon",
  txFeatureType = "exon",
  save_rdata = TRUE,
  rdata_file = NULL,
  save_txdb = TRUE,
  save_bed = TRUE,
  force_refresh = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{gtf}{\code{character} path to a GTF file. When \code{gtf} is
not supplied, \code{rdata_file} must be supplied, in which
case that file is used to load pre-existing \code{genome_regions}
GRanges object.}

\item{upstream_promoter, downstream_promoter}{\code{numeric} value
which defines the distance upstream and downstream relative
to each gene transcript start site, to be annotated as
a \code{"Promoter"}.}

\item{upstream_tts, downstream_tts}{\code{numeric} value
which defines the distance upstream and downstream relative
to each gene transcript termination site, to be annotated as
a \code{"TTS"}.}

\item{detectedTx}{\code{character} vector of transcripts which are
"detected" and therefore used in determining the relevant
gene-transcript annotations. This argument is intended to
allow using a subset of transcripts, as opposed to using all
annotated transcripts in the GTF file. When defined, it
forces the argument \code{save_rdata=FALSE}.}

\item{detectedGenes}{\code{character} vector of genes which are
"detected" and therefore used in determining the relevant
gene-transcript annotations. This argument can be used together
with \code{detectedTx} to define a specific subset of genes, using
only the subset of detected transcripts from those genes.
When defined, it forces the argument \code{save_rdata=FALSE}.}

\item{geneAttrNames}{\code{character} vector of attribute names to be
read from the GTF file in column 9, associated at the gene level.
The default values assume that \code{"gene_id"} and \code{"gene_name"}
attributes are defined in the GTF file.}

\item{txAttrNames}{\code{character} vector of attribute names to be
read from the GTF file in column 9, associated at the transcript
level. The default values assume that \code{"transcript_id"}
attribute is defined in the GTF file.}

\item{geneFeatureType, txFeatureType}{\code{character} vector indicating
the feature types as defined in column 3 of the GTF file, associated
to each gene, and transcript, respectively. These values are
specifically used by \code{splicejam::makeTx2geneFromGtf()} to create
a \code{data.frame} that associated transcripts to genes.}

\item{save_rdata}{\code{logical} indicating whether to save the
resulting R object in a file with the file extension
\code{".genomic_regions.RData"}.}

\item{rdata_file}{\code{character} filename, optionally used to
define an input file for \code{genome_regions}, or a specific
output file to save the RData object. When \code{force_refresh=TRUE}
this argument is used to save new RData to a file, without
re-using any pre-existing data saved in that file.}

\item{save_txdb}{\code{logical} indicating whether to save the
intermediate \code{Txdb} R object as a SQLite database, using
\code{AnnotationDbi::saveDb()}, using the file extension \code{".txdb"}.
When this option is enabled, any previously stored \code{TxDb}
will be re-used, unless \code{force_refresh=TRUE}.}

\item{save_bed}{\code{logical} indicating whether to save the
genome_regions also in BED format.}

\item{force_refresh}{\code{logical} indicating whether to force
a full refresh of the processing steps in this function.
When \code{force_refresh=TRUE}, the \code{rdata_file} input file
is not re-used, but is created and will overwrite
the \code{rdata_file} if it exists.
When \code{force_refresh=TRUE}: the GTF file will be read;
a new TxDb object will be created, optionally saved if
\code{save_txdb=TRUE}; and new RData file will be saved if
\code{save_rdata=TRUE}.}

\item{verbose}{\code{logical} indicating whether to print verbose output.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{GRanges} object
}
\description{
Prepare genomic regions from GTF
}
\details{
This function takes a GTF file as input, and creates
a \code{GenomicRanges} object that represents genomic regions.
By default, the resulting R object is saved in a file
with file extension \code{".genome_regions.RData"}, so that
the data can be loaded rapidly during subsequent analyses.

The main components of genomic regions defined by this function:
\itemize{
\item \code{"Promoters"}: defined with \code{upstream_promoter} distance upstream
of each gene transcript start site, and \code{downstream_promoter}
distance downstream each gene transcript start site. Note that these
distances are stranded, which means that negative-strand features
\code{strand="-"} will have these distances applied in the opposite
direction.
\item \code{"TTS"}: defined as \code{"Transcript Termination Site"} for each
gene transcript. A region around the TTS is defined by applying
\code{upstream_tts} and \code{downstream_tts} distances, in the same manner
as upstream and downstream distances are applied to \code{"Promoters"}.
\item \code{"Exons"}: defined by gene transcript exons, reduced into
one set of exons for each gene.
\item \code{"Introns"}: defined for each gene, using gene transcript ranges
(from the transcript start site through the transcript termination site)
then subtracting exons. Introns are therefore defined as regions
of a gene locus which are not annotated as exons in any associated
gene transcripts for that gene locus.
}

Any region of the genome which is not annotated as a Promoter, TTS,
exon, or intron, is referred to as \code{"extragenic"}, although extragenic
regions are not defined by this function directly. Note that we favor
the term \code{"extragenic"} over \code{"intergenic"} because: the latter term
may be confused with \code{"intragenic"}; and may imply a region that
must be between two existing genes, which is not the case.

In more detail: The arguments \code{geneAttrNames}, \code{txAttrNames}, and
\code{geneFeatureType} are used by \code{splicejam::makeTx2geneFromGtf()} to
create a \code{data.frame} which relates \code{transcript_id} to each \code{gene_name}
by default. If the GTF file uses different attributes, adjust
\code{geneAttrNames} and \code{txAttrNames} to match the GTF file.
}
\seealso{
Other slicejam genome regions: 
\code{\link{annotate_gr_by_genome_region}()},
\code{\link{flatten_genome_regions}()},
\code{\link{promoter_to_tss}()}
}
\concept{slicejam genome regions}
