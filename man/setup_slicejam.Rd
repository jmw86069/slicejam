% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slicejam-setup-scripts.R
\name{setup_slicejam}
\alias{setup_slicejam}
\title{Setup slicejam analysis scripts}
\usage{
setup_slicejam(
  output_path = ".",
  overwrite = FALSE,
  dryrun = TRUE,
  bash_shebang = "#!/usr/bin/env bash",
  wrapper_script = "run_slicejam.sh",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{output_path}{\code{character} file path to a directory where the
resulting \code{.R} and \code{.Rmd} files should be stored.}

\item{overwrite}{\code{logical} indicating whether to overwrite existing
files if they already exist in the \code{output_path} directory.}

\item{dryrun}{\code{logical} indicating whether to print commands without
performing the file copy steps.}

\item{bash_shebang}{\code{character} line used at the top of the output
script, traditionally called a "shebang". This line points
the script to a specific executable environment, by default
points to bash. This argument is only appropriate for linux- and
Unix-like operating systems.}

\item{verbose}{\code{logical} indicating whether to print verbose output.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{character} string invisibly returned, which contains
the full bash shell script.
}
\description{
Setup slicejam scripts for command-line analysis
}
\examples{
if (FALSE) {
   testdir <- file.path(tempdir(), "slicejam_testing")
   if (!dir.exists(testdir)) {
      dir.create(testdir)
   }
   # print steps to be taken without performing them
   setup_slicejam(testdir, dryrun=TRUE)
   
   # performing the steps
   setup_slicejam(testdir, dryrun=FALSE)
}

}
\seealso{
Other slicejam commandline:
\code{\link[=update_slicejam_shebang]{update_slicejam_shebang()}}
}
\concept{slicejam commandline}
