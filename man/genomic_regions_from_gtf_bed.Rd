% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slicejam-genomic-regions-from-gtf.R
\name{genomic_regions_from_gtf_bed}
\alias{genomic_regions_from_gtf_bed}
\title{Genomic Regions from GTF and BED}
\usage{
genomic_regions_from_gtf_bed(
  gtf,
  bed = NULL,
  upstream_promoter = 2000,
  downstream_promoter = 200,
  upstream_tts = 1000,
  downstream_tts = 1000,
  detectedTx = NULL,
  detectedGenes = NULL,
  mask = NULL,
  save_outfile = FALSE,
  outfile = NULL,
  save_rdata = FALSE,
  rdata_file = NULL,
  save_txdb = TRUE,
  force_refresh = FALSE,
  max_threads = 4,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{gtf}{\code{character} path to GTF file}

\item{bed}{\code{character} path to BED file, or \code{GRanges}.}

\item{upstream_promoter, downstream_promoter}{\code{integer} distance
upstream or downstream the gene transcript start site (TSS),
with default 2000, 200, respectively.}

\item{upstream_tts, downstream_TTS}{\code{integer} distance
upstream or downstream the gene transcript termination site
(TTS), with default 1000, 1000, respectively.}

\item{detectedTx}{\code{character} path to file with detected
transcripts, which match 'transcript_id' in the GTF file.}

\item{detectedGenes}{\code{character} path to file with detected
genes, which match 'gene_name' in the GTF file.}

\item{mask}{\code{character} path to BED file with mask regions, or
\code{GRanges} object.}

\item{save_outfile}{\code{logical}, default FALSE, whether to save
the results to an annotated BED file.}

\item{outfile}{\code{character} output file, default NULL uses the BED
filename to create a filename with \code{'.genome_regions.'}, the
current date, and ending \code{'.txt'}.}

\item{save_rdata}{\code{logical} default FALSE, whether to save data
as an RData file. It uses the \code{'outfile'} and replaces 'txt'
with 'RData'.}

\item{rdata_file}{\code{character} default NULL, to specify a specific
RData output file.}

\item{save_txdb}{\code{logical} default TRUE, whether to save the
\code{TxDb} created from the GTF file. This step saves time when
using the same GTF in future.}

\item{force_refresh}{\code{logical} default FALSE, forces the GTF
to be reprocessed, therefore not re-using any \code{TxDb} data.}

\item{max_threads}{\code{integer} threads used by 'data.table',
default 4.}

\item{verbose}{\code{logical} whether to print verbose output.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{list} with supporting data objects.
The key object is 'bed_gr' containing the BED data
annotated with genome regions.
\itemize{
\item 'genome_regions': \code{GRanges} with genomic regions
as defined by the other arguments.
\item 'mask': \code{GRanges} only when provided as a BED file
or \code{GRanges} object.
\item 'bed_gr': \code{GRanges} of 'bed' data annotated using
the genomic regions.
\item 'outfile': the output file when 'save_outdata=TRUE'.
\item 'outfile_rdata': the output RData file when
'save_rdata=TRUE'.
}
}
\description{
Genomic Regions from GTF and BED
}
\details{
This function represents the 'Rscript' functionality
of 'genomic_regions_from_gtf', by wrapping:
\itemize{
\item \code{genomic_regions_from_gtf()} which creates the
genomic regions from GTF, using detected genes and
transcripts when provided, and using custom promoter
and TTS region sizes.
\item \code{annotate_gr_by_genome_region()} which uses the
BED file data (GRanges) and annotates them with
the genomic regions.
}

Note the Rscript will prepare genome regions when
there is no BED file. If you have no BED file to use,
use \code{genomic_regions_from_gtf()} which does not use
the BED file.
}
\seealso{
Other slicejam genome regions:
\code{\link[=annotate_gr_by_genome_region]{annotate_gr_by_genome_region()}},
\code{\link[=flatten_genome_regions]{flatten_genome_regions()}},
\code{\link[=genomic_regions_from_gtf]{genomic_regions_from_gtf()}},
\code{\link[=promoter_to_tss]{promoter_to_tss()}},
\code{\link[=salmon_tss_per_gene]{salmon_tss_per_gene()}}
}
\concept{slicejam genome regions}
