% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jam-best-tss-per-gene.R
\name{salmon_tss_per_gene}
\alias{salmon_tss_per_gene}
\title{Define the best TSS per gene from 'Salmon' quant data}
\usage{
salmon_tss_per_gene(
  txiTx,
  groups = NULL,
  tx2tss = NULL,
  tx2tts = NULL,
  gtf = NULL,
  detectedGenes = NULL,
  detectedTx = NULL,
  countsFromAbundance = "lengthScaledTPM",
  use_signal = c("counts", "abundance"),
  return_type = c("tss", "bed", "se"),
  pct_thresholds = c(50, 20),
  remove_genebody = TRUE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{txiTx}{\code{list} or \code{SummarizedExperiment} with all assay names:
\code{c("counts", "abundance", "length")}}

\item{groups}{\code{character} (default NULL) used to define sample groups,
useful to use the max group mean instead of global mean.
\itemize{
\item Using proper \code{groups} is recommended.
\item It can use a column in \code{colData(txiTx)} when supplying a
\code{SummarizedExperiment} object.
}}

\item{tx2tss}{\code{data.frame} that must contain two columns:
\itemize{
\item \code{"transcript_id"} - which matches rownames of txiTx.
\item \code{"tss"} - in the exact form \code{"[gene_name]_[seqnames]_[start]_[strand]"}
for example \code{"5_8S_rRNA_chr14_16057472"} is recognized to have these
fields: \verb{"5_8S_rRNA", "chr14", "16057472", "+"}
}}

\item{tx2tts}{\code{data.frame} with TTS sites, with the same
format as 'tx2tss'.}

\item{gtf}{\code{character} path or URL to a GTF file, used when both
'tx2tss' and 'tx2tts' are NULL.
\itemize{
\item In this case, the GTF is used
to define the TSS and TTS sites for each transcript, for each
gene locus.
\item The GTF is processed internally by \code{convert_gtf_to_txdb()}
and \code{convert_gtf_to_tx2gene()} both of which convert the GTF
to \code{TxDb}, and either load a previous \code{'.txdb'} file for
this purpose, or will create and save this file for future use.
Similarly, the 'tx2gene' will be saved to '.tx2gene.txt'
for future use.
\item Data are subset by 'detectedGenes' and 'detectedTx'
if provided.
\item When both TSS and TTS data are defined, it follows rules
described in the 'Details' section.
}}

\item{countsFromAbundance}{\code{character} string passed to
\code{tximport::summarizeToGene()}, default 'lengthScaledTPM'.}

\item{use_signal}{\code{character} string indicating which measure of expression
to use:
\itemize{
\item \code{"counts"} - uses data in the \code{counts} matrix, default.
\item \code{"abundance"} - uses data in the \code{abundance} matrix, TPM.
}}

\item{return_type}{\code{character} type of data to return:
\itemize{
\item \code{"tss"} (default) returns a \code{data.frame} with one TSS per gene
for genes with non-zero observed expression.
\item \code{"bed"} as above, returns a \code{data.frame} with one TSS per gene
for genes with non-zero observed expression, using BED columns.
\item \code{"se"} returns \code{SummarizedExperiment} with all TSS rows included,
and \code{rowData()} which contains annotations for each unique TSS.
}}

\item{pct_thresholds}{\code{numeric} values with optional thresholds to
indicate when a particular TSS met a percent-max for the given gene.
\itemize{
\item The defaults \code{c(50, 20)} indicate whether a TSS had expression at
least 20\% or 50\% of the max observed expression.
\item The calculation is done in normal space, with exponentiated data
using: \code{(2^x - 1)}
}}

\item{remove_genebody}{\code{logical} default TRUE, whether to remove
transcripts recognized as full gene body (unspliced), which
also have \code{"_gb"} suffix.}

\item{verbose}{\code{logical} indicating whether to print verbose output.}

\item{...}{additional arguments are passed to \code{jamba::rowGroupMeans()}
for example one may customize with \code{useMedian=TRUE} to calculate
group median expression.}

\item{detected_genes}{\code{character} (default NULL) with optional subset
of genes that are considered "detected" by some criteria.
The TSS results will only include detected genes by default.
See \code{jamses::se_detected_rows()} (Github 'jmw86069/jamses')
for an example of heuristics to define detected genes in
bulk RNA-seq data.}
}
\value{
Dependent upon the input arguments:
\itemize{
\item When 'gtf' is provided without 'tx2tss' nor 'tx2tts',
or when both 'tx2tss' and 'tx2tts' are provided, both the
TSS and TTS sites are defined.
The output will be a \code{list} with names 'TSS' and 'TTS',
containing the object type as defined below.
\item The object type is defined by argument \code{return_type}:
\itemize{
\item 'tss': \code{data.frame} default.
\item 'se': \code{SummarizedExperiment} using the input \code{txiTx} when also
supplied as \code{SummarizedExperiment}. Useful with 'TxSE' produced
from \code{platjam::import_salmon_quant()} (Github 'jmw86069/platjam')
for example.
\item 'bed': \code{data.frame} in BED column format, suitable to save
as BED file (without headers) or convert to \code{GRanges} directly, using
\code{as(bed, "GRanges")} via the package GenomicRanges.
}
}
}
\description{
Define the best TSS per gene from 'Salmon' quant data,
using transcript quantitation produced by \code{tximport::tximport()}
to determine the TSS and TTS with the highest supporting
signal.
}
\details{
'Salmon quant' produces estimated quantitation for
individual transcript isoforms, whose data are most often
imported using \code{tximport::tximport()}. As such, similar
tools such as 'kallisto' are also capable of providing
equivalent input data.

Abundance estimates include transcripts in the corresponding
transcriptome index file used by 'Salmon' or 'kallisto'.
For the purpose of the workflow used here, the data are
imported using \code{tximport::tximport()}.
We typically use \code{countsFromAbundance='lengthScaledTPM'}
however, the abundance itself can be used and not the
estimated pseudo-counts.

The workflow essentially re-uses \code{tximport::summarizeByGene()}
where 'transcript_id' values are grouped by gene and transcript
start site (TSS) to aggregate abundance estimates by TSS.
Then the highest abundance TSS is selected as "preferred".
\subsection{Handling both TSS and TTS}{

When providing only the 'gtf', it will calculate all TSS
and TTS sites, and both will be processed.

When both TSS and TTS sites are provided, it performs the
analysis in tandem:
\enumerate{
\item It determines the 'best TSS' site for each gene.
\item It identifies all transcripts which have these TSS sites.
\item It subsets TTS 'tx2tts' to use only these transcripts,
so that the 'best TTS' site will only be evaluated using
transcripts compatible with the best 'TSS' site.
}

There are rare instances where the 'best TTS' site may
be upstream from the 'best TSS' site, which does not make
sense. While this may be true, that a particular TTS has
more signal than a downstream TSS site annotated to the
same gene locus, for the purpose of this method, the
TTS is assumed to represent transcription that originated
from the 'best TSS' site.

To retain the 'best TTS' independent of the 'best TSS',
process the TSS \code{'tx2tss'} and the TTS \code{'tx2tts'} data
in two independent steps.
}

\subsection{Additional Comments}{
\itemize{
\item Note that \code{"transcript_id"} values are not returned.
There are often many transcript \code{'transcript_id'}
with the same TSS.
The transcripts are not essential to this processing stage.
\item It is possible to recover the transcripts by reviewing
the \code{'tx2tss'} column 'tss', and comparing with the
'best TSS' for a particular gene.
}
}
}
\examples{
jamsession::refresh_functions("salmontsspergene")
tss_df638 <- salmon_tss_per_gene(TxSE638, groups="Group", tx2tss=tx2tss, detected_genes=env638$detected_genes, verbose=TRUE)

}
\seealso{
Other slicejam genome regions:
\code{\link[=annotate_gr_by_genome_region]{annotate_gr_by_genome_region()}},
\code{\link[=flatten_genome_regions]{flatten_genome_regions()}},
\code{\link[=genomic_regions_from_gtf]{genomic_regions_from_gtf()}},
\code{\link[=genomic_regions_from_gtf_bed]{genomic_regions_from_gtf_bed()}},
\code{\link[=promoter_to_tss]{promoter_to_tss()}}
}
\concept{slicejam genome regions}
