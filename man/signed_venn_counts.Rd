% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slicejam_venn.R
\name{signed_venn_counts}
\alias{signed_venn_counts}
\title{Define directional Venn counts}
\usage{
signed_venn_counts(
  setlist,
  return_type = c("counts", "names", "im", "setlist_arrows"),
  sep = "-",
  alt_sep = "!",
  pos_col = "red4",
  neg_col = "blue4",
  mixed_col = NULL,
  verbose = FALSE,
  ...
)
}
\description{
Define directional Venn counts
}
\examples{
L1 <- list(A=c("C","A","B","A"),
   D=c("D","E","F","D"),
   A123=LETTERS[c(1:8,3,5)],
   T=LETTERS[7:9]);
L1;
# Convert each vector to a signed vector
set.seed(123);
L2 <- lapply(L1, function(i){
   i <- unique(i);
   jamba::nameVector(sample(c(-1,1), size=length(i), replace=TRUE), i);
});
L2;

signed_venn_counts(L2)
signed_venn_counts(L2, return_type="setlist_arrows")

}
